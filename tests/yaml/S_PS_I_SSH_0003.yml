pipelines:
  - name: pipelines_S_PS_I_SSH_0003
    steps:
      - name: S_PS_I_SSH_0003
        type: PowerShell
        configuration:
          integrations:
            - name: PSSI_SSHKEY
        execution:
          onExecute:
            - echo "executing step..."
