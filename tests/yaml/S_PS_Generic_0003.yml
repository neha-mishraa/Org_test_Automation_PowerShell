pipelines:
  - name: S_PS_Generic_0027
    steps:
      - name:
        type: PowerShell
        configuration:
          nodePool: win_2019
        execution:
          onExecute:
            - write-output "executing step..."
