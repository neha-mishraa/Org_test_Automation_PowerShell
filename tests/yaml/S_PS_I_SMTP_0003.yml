pipelines:
  - name: pipelines_S_PS_I_SMTP_0003
    steps:
      - name: S_PS_I_SMTP_0003
        type: PowerShell
        configuration:
          integrations:
            - name: PSSI_SMTP
        execution:
          onExecute:
            - echo "executing step..."
